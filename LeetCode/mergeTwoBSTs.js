class TreeNode {
    constructor(val) {
    this.val = val;
    this.left = this.right = null;
    }
}

function mergeTrees(t1, t2, mergeTree = new TreeNode(t1.val + t2val)) {
    if ((t1.left) && t2.left && (!t1.right && !t2.right)) {
        
    }
}

